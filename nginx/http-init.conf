server {
    listen 80;
    server_name kuantile.com www.kuantile.com api.kuantile.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 "kuantile.com - kurulum devam ediyor";
        add_header Content-Type text/plain;
    }
}
